{
  "name": "test-polyer-lit-element",
  "version": "1.0.0",
  "description": "testing polymer",
  "main": "index.js",
  "author": "PS",
  "license": "MIT",
  "scripts": {
    "start": "yarn run start:polymer | yarn run start:browser-sync",
    "start:polymer": "polymer serve --compile never --port 8042 --hostname 127.0.0.1",
    "start:browser-sync": "browser-sync start --proxy 127.0.0.1:8042 --files 'src/*.html, src/*.js'"
  },
  "dependencies": {
    "@polymer/lit-element": "^0.6.2"
  }
}
